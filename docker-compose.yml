version: '3.9'

services:
  app:
    container_name: lara-lw
    build: .
    ports:
      - '8000:8000'
    volumes:
      - .:/home/app
    networks:
      - common-network
volumes:
  app:
networks:
  common-network:
    driver: bridge
